# fa-console
fa-console — Correct Persian (Farsi) console I/O for Python on Windows. One import fixes both layers: automatic UTF-8 setup (code page, standard streams, child processes) and visual letter shaping + bidirectional reordering for the classic console. Zero dependencies, fail-open, live correct echo while typing.
